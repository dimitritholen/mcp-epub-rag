# Code Examples

Here's some JavaScript:

```javascript
function hello(name) {
  console.log(`Hello, ${name}!`);
}
```

And some Python:

```python
def greet(name):
    print(f"Hello, {name}!")
```

Inline `code` is also supported.